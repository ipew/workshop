<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.org.eldorado.workshop2">

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivitiesFirstActivity" />
        <activity android:name=".ActivitiesSecondActivity" />
        <activity android:name=".ServiceActivity" />
        <activity android:name=".GridActivity" />
        <activity
            android:name=".fragments.FragmentExampleActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait" />

        <service android:name=".WorkshopService" />

        <activity android:name=".SensorActivity" android:theme="@style/SensorsStyle" />
    </application>
</manifest>
